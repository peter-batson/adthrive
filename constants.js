const path = require('path');

module.exports = {
    POSTS_CSV: './output/posts.csv',
    TOP_POSTS_CSV: './output/top_posts.csv',
    OTHER_POSTS_CSV: './output/other_posts.csv',
    DAILY_TOP_POSTS_CSV: './output/daily_top_posts.csv',
    TOP_POSTS_JSON: './output/top_posts.json',
    OTHER_POSTS_JSON: './output/other_posts.json',
    DAILY_TOP_POSTS_JSON: './output/daily_top_posts.json'
}